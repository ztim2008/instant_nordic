# Nordic AI / Users — Worklog 2026-09-09

## Контекст дня

- Рабочий сайт: `nordic-builder.store` (шаблон отдачи `modern`).
- Ветка: `feature/nordicstyl-picker`.
- Цель: проверить гипотезу AI skin-агента (DeepSeek → tokens/scoped CSS) + живой инспектор на фронте; попутно каталог карточек `/users`.

Канонический handoff для нового агента:  
[HYPOTHESIS-MVP-HANDOFF-2026-09-09.md](./HYPOTHESIS-MVP-HANDOFF-2026-09-09.md)

## Что сделано

### 1. Компонент `nordicai` (MVP)

- Контроллер: `system/controllers/nordicai/`
- Язык RU, backend agent UI, options (DeepSeek key/model/timeout, strict tokens, allow CSS)
- `PatchService` + `DeepSeekClient`
- Hook `before_print_head`: overlay для всех, inspector только админу
- Frontend AJAX: `generate`, `overlay_save`, `overlay_clear`
- Таблица `cms_nordicai_runs`
- Package snapshot: `packages/nordicai/`
- Skill: `.cursor/skills/nordicai-agent/SKILL.md`

### 2. Live inspector UX

- FAB «Выбрать элемент» → pick DOM → prompt → Generate → Preview → Save
- Overlay пишется в options `site_overlay_css`

### 3. Баг JSON / 404 (закрыт)

- `empty($this->cms_user->is_admin)` через magic `__get` давал ложный deny → HTML 404 → `SyntaxError` в inspector
- Исправлено на `cmsUser::getInstance()` + JSON `forbidden`
- JS: безопасный разбор ответа (не JSON → понятная ошибка)

### 4. `/users` card grid

- `templates/modern/controllers/users/list.tpl.php` + `styles.css` (+ scss)
- Зеркало стилей/листа в `templates/nordic/controllers/users/`

### 5. Гипотеза подтверждена пользователем

- Точечные правки элементов работают, сайт меняется через overlay.
- Ограничение: большой parent/wrapper — модели не хватает внутреннего контекста (зафиксировано в handoff как next step, без реализации сегодня).

## Что проверено

- Гость: hook `admin=0`, inspector нет; `POST /nordicai/generate` → JSON `forbidden`
- Админ: FAB виден; generate → `ok:true` + patch от DeepSeek (~1–2s)
- overlay_save / overlay_clear → `ok:true`
- Ручной smoke пользователем: гипотеза «всё меняется» подтверждена

## Риски на конец дня

- Крупные селекторы / обёртки: нужен richer context (auto-narrow / DOM snapshot) — не сделано
- API key хранится в options БД; ключ когда-то светился в чате — желательна ротация на стороне DeepSeek
- Временный пароль админа мог меняться при smoke — сверить доступ пользователя
- В working tree остаются посторонние WIP (MCP symlink/bak, nordicblocks about, roadmap) — **не** входят в этот день-коммит
- `system/config/config.php` (production_time и пр.) не коммитится

## Закрытие дня

1. **Что изменено:** MVP `nordicai` + live inspector + fix admin/empty JSON bug; redesign `/users` cards; docs handoff + worklog.
2. **Что проверено:** admin generate/save/clear; guest forbidden; user confirmation hypothesis works.
3. **Какие риски остались:** large-wrapper context; key rotation; unrelated WIP left unstaged.
4. **Точка отката:** git commit дня на `feature/nordicstyl-picker` (см. hash после push); SQL schema только `cms_nordicai_runs` (откат компонента = disable controller + clear options overlay).

## Следующий день (если продолжать nordicai)

1. Auto-narrow / compact DOM context для крупных узлов
2. Ротация DeepSeek key при необходимости
3. Опционально: UI истории overlay / откат ревизий

## Related idea

- [TEMPLATE-INSPECTOR-KIT-IDEA-2026-09-10.md](./TEMPLATE-INSPECTOR-KIT-IDEA-2026-09-10.md) — controllable templates product line (idea only).
