# NordicBlocks — Старт на 2026-04-18

## Короткий release-summary

Ветка: `feature/nordicstyl-picker`

Checkpoint перед clean-stand closeout: `aa8506576fa96476558e0757f33135c56a94fde8`

Что вошло в срез:

1. Day 5 доведён до composition-shell состояния: `editor_hero_v2.tpl.php` теперь собирается из partial-слоёв вместо одного giant runtime-файла.
2. В отдельные partial-файлы вынесены renderer groups, control dispatch, shell events, panel selection helpers и control UI helpers.
3. Runtime и package mirror синхронизированы на одной и той же partial-структуре.
4. Live-smoke на свежих `faq` и `hero` подтвердил reload, entity/tab/mobile transitions и сохранение после финального shell-split.
5. Временные smoke-блоки `84` и `85` после проверки удалены, список блоков снова пустой.

Что проверено перед закрытием дня:

1. PHP syntax check по main shell и всем новым partial-файлам в runtime и package mirror.
2. `Problems`/editor diagnostics по затронутым файлам.
3. Live browser-smoke по fresh `faq` и `hero` с проверкой reload, entity/tab/mobile и save.
4. Штатное удаление smoke-блоков через backend `block_delete`.

## План на день

1. Дожать Day 6: полностью выровнять `hero` и `faq` на одном каноническом editor flow без скрытых block-specific admin-развилок.
2. Отдельным проходом взять документ `EXTENDED-DESIGN-SETTINGS-2026-04-18.md` как рабочую карту по расширенным visual controls: фон, поверхности, радиусы, тени, отступы и spacing.
3. Провести аудит оставшихся `hero`/`faq`-specific admin templates, entry points и compatibility glue, отделив нужное от уже лишнего.
4. Зафиксировать, какие шаблоны и runtime-ветки считаются каноном, а какие готовы к выводу из активного потока.
5. Повторить короткий smoke уже не на временных блоках, а на реальном каноническом потоке после следующего шага унификации.
6. Если хватит прохода, подготовить список безопасных legacy-cut кандидатов для Day 7 cleanup/freeze.

## Что не забыть утром

1. Базовая rollback-точка на старт: `aa8506576fa96476558e0757f33135c56a94fde8`.
2. Snapshot: `snapshot/20260417-194139`.
3. DB backup: `backups/db/builders-20260417-194138.sql.gz`.
4. Стенд чистый: каталог блоков пуст, временные smoke-id `84` и `85` уже удалены.
5. Отдельный рабочий документ по расширенным настройкам: `docs/nordicblocks/EXTENDED-DESIGN-SETTINGS-2026-04-18.md`.

## Точка возврата

1. Основной rollback-ориентир: checkpoint `aa8506576fa96476558e0757f33135c56a94fde8`.
2. Snapshot checkpoint: `snapshot/20260417-194139`.
3. DB backup checkpoint: `backups/db/builders-20260417-194138.sql.gz`.