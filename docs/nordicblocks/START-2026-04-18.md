# NordicBlocks — Старт на 2026-04-18

## Короткий release-summary

Ветка: `feature/nordicstyl-picker`

Коммит закрытия дня: `0dcafa64b33fc6ea54211f4ccd00a13d41092095`

Что вошло в срез:

1. Каталог блоков получил product-level карточки с real preview или schematic fallback.
2. Поток размещения переведен на нативный путь через `Структура -> Виджеты` с авто-открытием нужной вкладки и подсветкой `NordicBlocks Block`.
3. Переименование блока теперь синхронизируется с widget bind, а удаление чистит связанные widget bindings и bind pages.
4. Preview canvas переведен на autosize по реальной высоте iframe, чтобы убрать nested scroll и эффект `окно внутри окна`.
5. Добавлен отдельный migration-план перехода к unified inspector runtime.
6. В начало главного dev-документа добавлена короткая продуктовая концепция NordicBlocks как InstantCMS-native addon.

Что проверено перед закрытием дня:

1. PHP syntax check по затронутым backend/model файлам.
2. `scripts/nordicblocks-flow-smoke.php`.
3. `scripts/nordicblocks-sync.php` в dry-run режиме.
4. Точечная browser-проверка проблемного mobile preview без горизонтального overflow.

## Хвосты на следующий день

Список собран из [docs/nordicblocks/WORKLOG-2026-04-17.md](docs/nordicblocks/WORKLOG-2026-04-17.md).

1. Дожать полный переход всех block editors на один universal inspector runtime: registry/state runtime уже есть, но legacy block-specific слой еще не вымыт полностью.
2. Повторить короткий browser-smoke по всем актуальным block types после последних правок editor shell.
3. Формализовать closeout-практику для новых worklog-документов в отдельное жесткое repo-policy правило, чтобы это не оставалось только устной нормой.

## Точка возврата

1. Основной rollback-ориентир: коммит `0dcafa64b33fc6ea54211f4ccd00a13d41092095`.
2. Предыдущая файловая checkpoint-точка: `backups/checkpoints/20260407-145258-autoscale-inline-toggle-and-preview-smoke`.