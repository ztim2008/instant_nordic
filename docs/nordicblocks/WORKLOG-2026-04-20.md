# NordicBlocks — Worklog 2026-04-20

## Universal Visual Inspector Day 8 baseline and second-wave selection

- День начат строго по [docs/nordicblocks/START-2026-04-20.md](docs/nordicblocks/START-2026-04-20.md): сначала regression baseline, потом выбор только одного следующего block type.
- Короткий regression smoke по pilot editor blocks `#121–#125` не показал явной деградации shared inspector loop.
- На editor baseline подтверждено:
  - `#121 hero` открывается и держит entity selection;
  - `#122 faq` открывается и держит selection по `eyebrow`;
  - `#123 content_feed` открывается и держит selection по карточечному CTA;
  - `#124 catalog_browser` открывается, shared entity chips и selection flow живы;
  - `#125 swiss_grid` открывается и держит selection по карточечному CTA.
- Public sanity-check прошёл без нового временного размещения smoke block:
  - главная страница не отдаёт `Warning`, `Notice` или `Fatal error`;
  - временный widget bind для block `124` не вернулся;
  - текущее состояние главной соответствует baseline, а не вчерашнему smoke placement.
- SQL-изменений и нового rollback шага сегодня не делалось.
- Актуальная точка отката остаётся прежней: `snapshot/20260419-211558` + `backups/db/builders-20260419-211557.sql.gz`.

## Выбор следующего block type для второй волны

- Для второй волны выбран `headline_feed`.
- Причины выбора:
  - блок добавляет новый полезный сценарий `lead + rail/grid`, а не повторяет уже закрытый pilot `content_feed`;
  - у блока уже есть manifest-first contract, normalizer support, shared shell profile, layout controls и runtime CSS;
  - rollout не требует нового private inspector shell и укладывается в shared contour `manifest -> normalizer -> shared renderers -> SSR -> runtime CSS`.
- `category_cards` сознательно не взят следующим, потому что на этом шаге он слишком близок к уже закрытому пути `content_feed + eyebrow` и слабее расширяет доказанную shared system.

## Что сделано после выбора

- Собрана discovery-only rollout-map для `headline_feed` в отдельном документе [docs/nordicblocks/HEADLINE-FEED-ROLLOUT-MAP-2026-04-20.md](docs/nordicblocks/HEADLINE-FEED-ROLLOUT-MAP-2026-04-20.md).
- На уровне discovery уже подтверждено, что у `headline_feed` есть по крайней мере один truthful gap: manifest объявляет `itemLink`, но SSR markup пока не поднимает эту entity как отдельный кликабельный node.
- Также подтверждено, что для `headline_feed` сейчас нет готовых block instances в таблице `nordicblocks_blocks`, поэтому будущий smoke потребует временного block creation/placement и последующего cleanup.

## Статус на конец текущего прохода

- Step 2 из стартового плана фактически закрыт: regression baseline зелёный.
- Step 3 закрыт: выбран один block type для второй волны.
- Step 4 закрыт в discovery-режиме: rollout-map собрана до кодовых изменений.

## Headline Feed itemLink rollout

- Перед implementation создан checkpoint `snapshot/20260420-051136`, DB backup — `backups/db/builders-20260420-051135.sql.gz`.
- По продуктовому решению `itemLink` для `headline_feed` оставлен как реальная canonical entity, а не снят из manifest.
- Доведён shared-path rollout для `headline_feed` в live и package mirror:
  - в schema/repeater добавлен `link_label` для карточечного CTA;
  - в `headline_feed` UI profile добавлены `itemTypography.link` defaults;
  - SSR render теперь нормализует `linkLabel`, эмитит `--nb-feed-item-link-*` variables и рисует честные `data-nb-entity="itemLink"` nodes в карточках.
- Проведён editor smoke на временном block `#126 Day8 Headline Feed ItemLink Smoke`:
  - headline runtime показал CTA в preview;
  - после обычного scroll+click по CTA chip `CTA карточки` стал active в inspector;
  - это подтверждает не только markup presence, но и живой canvas -> entity -> inspector loop для `itemLink`.
- Временный smoke block `#126` удалён после проверки; хвостов в `nordicblocks_blocks` не осталось.

## Статус на конец текущего прохода

- Step 2 из стартового плана закрыт: regression baseline зелёный.
- Step 3 закрыт: выбран один block type для второй волны.
- Step 4 закрыт: rollout-map была собрана и затем подтверждена implementation smoke.
- На конец этого прохода у `headline_feed` закрыт itemLink path через shared system без появления private inspector logic.

## Headline Feed editor save/reload closure

- Для editor-first smoke создан временный block `#127 Editor Smoke Headline Feed Day2`, затем удалён после проверки.
- В live editor подтверждён полный content loop для `CTA карточки`: выбор из canvas, правка текста, save/reload.
- Во время desktop/mobile smoke найден и закрыт backend contract gap для `itemLink` typography:
  - `BlockContractNormalizer` в live и package mirror дополнен полями `item_link_*` для feed-family denormalize/normalize roundtrip;
  - благодаря этому `design.entities.itemLink.desktop/mobile.*` перестали теряться на save.
- После этого найден и закрыт runtime CSS gap для `headline_feed/content_feed` CTA typography:
  - shared `blocks.css` в source и template copies переведён на `--nb-feed-item-link-*` desktop vars;
  - в mobile media rule добавлены `--nb-feed-item-link-*-mobile` overrides для `.nb-content-feed__item-link`.
- Финальная live проверка для block `#127` перед cleanup:
  - desktop CTA persisted как `16px` в contract и как `16px` в preview iframe;
  - mobile CTA persisted как `14px` в contract и как `14px` в preview iframe.
- Итог: `headline_feed` доведён в shared editor до состояния `поменял -> сохранил -> перезагрузил`, предсказуемого и на desktop, и на mobile, без block-private editor logic.

## Hero Panels Wide public runtime re-validation

- Проведена повторная live-проверка `hero_panels_wide` на block `#120` уже в текущем состоянии рабочей БД и публичной homepage.
- Во время проверки подтверждено, что у блока есть существующий baseline widget bind `#216` на homepage (`page_id = 1`, `position = pos_33`), поэтому отдельный временный placement для smoke больше не требуется.
- Для проверки штатного cleanup был кратко создан дополнительный временный bind `#221` через `block_place`, после чего он сразу удалён через стандартный `widgets_delete`; временный `bind_page #1033` и сам bind `#221` в БД не остались.
- На публичной странице после cleanup подтвержден server-rendered runtime path для baseline placement block `#120`:
  - блок присутствует в DOM как `#block-widget_block_120`;
  - scope marker `data-nb-block-root="hero_panels_wide"` присутствует;
  - published overlay присутствует как `style[data-nb-block-css-overlay="widget_block_120"]`;
  - заголовок `Мы создаём / элегантные решения QA` и кнопка `Связаться` рендерятся без деградации;
  - computed style заголовка на текущем runtime дал `font-size: 48px`, `letter-spacing: -1.92px`, `line-height: 52.8px`;
  - `Fatal error`, `Warning` и `Notice` на homepage не обнаружены.
- Документация family registry выровнена с фактическим состоянием: для `hero_panels_wide` `Live smoke` больше не `no`, а `yes`.

## Swiss Grid public widget smoke

- Для `swiss_grid` использован существующий live block `#125` (`Day5 Swiss Grid Smoke`) без создания нового block instance.
- Перед smoke подтверждено baseline-состояние: bind records для block `#125` отсутствовали, а на публичной homepage не было `#block-widget_block_125` и `style[data-nb-block-css-overlay="widget_block_125"]`.
- Блок был штатно размещён через `block_place/125`; создан временный bind `#222` и `bind_page #1034` в `pos_38`.
- Public runtime smoke на homepage подтвердил:
  - блок появился как `#block-widget_block_125`;
  - заголовок `Swiss Style Grid` и подзаголовок `Минимализм. Порядок. Типографика.` отрендерились;
  - в DOM присутствуют `itemLink` и `meta` сущности, первый card CTA `Подробнее` получил runtime style `font-size: 12.16px`, `line-height: 18.24px`, `display: flex`;
  - `Fatal error`, `Warning` и `Notice` на публичной странице отсутствуют.
- После smoke временный bind `#222` удалён штатным `widgets_delete`, в БД bind records для block `#125` снова отсутствуют, а homepage вернулась в baseline без блока `#125` и без overlay-хвостов.
- Family registry для `swiss_grid` обновлён: `Live smoke` теперь `yes`.

## Hero Panels Editorial public widget smoke

- Для `hero_panels_editorial` в текущей БД не было готового live instance, поэтому для smoke создан временный block `#129` (`Public Smoke Hero Panels Editorial Day2`) через model create/save path и `BlockContractNormalizer` с дефолтами из block schema.
- Перед placement подтверждено baseline-состояние: bind records для block `#129` отсутствовали, а на публичной homepage не было `#block-widget_block_129`.
- Блок был штатно размещён через `block_place/129`; создан временный bind `#223` и `bind_page #1035` в `pos_38`.
- Public runtime smoke на homepage подтвердил:
  - блок появился как `#block-widget_block_129`;
  - eyebrow `EDITORIAL HERO`, заголовок `Точный ритм / первого экрана`, subtitle, body copy, CTA `Обсудить проект` и изображение отрендерились без ручной доводки;
  - computed style заголовка на runtime дал `font-size: 92px`, `line-height: 101.2px`;
  - `Fatal error`, `Warning` и `Notice` на публичной странице отсутствуют.
- После smoke временный bind `#223` удалён штатным `widgets_delete`, затем временный block `#129` удалён через `block_delete`; в БД не осталось ни block record, ни widget bind records для `#129`, а homepage вернулась в baseline.
- Family registry для `hero_panels_editorial` обновлён: `Live smoke` теперь `yes`.

## Next candidate after closed smoke line

- После закрытия `headline_feed`, `swiss_grid`, `hero_panels_wide` и `hero_panels_editorial` повторно не открываем старые smoke-хвосты без нового бага или новой архитектурной причины.
- Следующим новым продуктовым кандидатом выбран news/editorial block `Компактная бенто-лента` с техническим slug `bento_feed`.
- Для него зафиксирован отдельный spec-first документ [docs/nordicblocks/BENTO-FEED-V1.md](docs/nordicblocks/BENTO-FEED-V1.md): controlled bento grid, InstantCMS `content_list`, отдельные desktop/mobile column settings и нижний режим `Показать ещё` как основной long-list path.
- Первый практический шаг по этому блоку зафиксирован как `checkpoint -> scaffold bento_feed на profile card_collection -> validator -> controlled implementation`, а не ручное копирование старого block directory.

## Bento Feed scaffold bootstrap

- Перед генерацией нового блока создан checkpoint `snapshot/20260420-070131`, DB backup — `backups/db/builders-20260420-070130.sql.gz`.
- Новый managed block type `bento_feed` поднят штатно через `scripts/nordicblocks-scaffold-block.php` с параметрами `family=news-family-v1`, `profile=card_collection`, `title=Компактная бенто-лента`.
- Scaffold создал live/package skeleton для `manifest.php`, `schema.json`, `render.php` и автоматически обновил docs trail:
  - `docs/checklists/NEW_BLOCK_CHECKLIST_STATUS.json`;
  - scaffold registry в `docs/nordicblocks/NEWS-FAMILY-V1.md`.
- Существующий validator пройден: `scripts/nordicblocks-validate-block.php --block=bento_feed` вернул `PASS`.

## Bento Feed implementation and smoke

- `bento_feed` доведён от scaffold skeleton до реального SSR news/editorial блока в live и package mirror:
  - schema переписана под controlled bento product shape с preset `editorial_mix/feature_stack`, desktop/mobile columns, `load_more`/`pagination`, visibility toggles и starter demo items;
  - render переписан под managed contract-first runtime с `content_list` hydration, entity markers, controlled bento grid и нижней long-list navigation;
  - для managed scaffold normalizer заранее закрыт round-trip по `itemLink`, `collectionMode`, `itemsPerPage`, `initialItemsCount`, `loadMoreLabel`, `showBottomNavigation` и `layout.preset`.
- После реализации ещё раз прогнан validator: `scripts/nordicblocks-validate-block.php --block=bento_feed` вернул `PASS`.
- Для воспроизводимого e2e smoke добавлен отдельный script [scripts/nordicblocks-bento-feed-content-list-smoke.php](scripts/nordicblocks-bento-feed-content-list-smoke.php).
- Скрипт подтвердил полный проход на живой системе:
  - создан временный block `#132`;
  - сохранены и перечитаны contract/runtime поля `collectionMode=load_more`, `columns_desktop=3`, `columns_mobile=1` и `content_list` source;
  - hydration реально подменила manual fallback items живыми записями InstantCMS из `articles`;
  - SSR render отдал grid markup и маркер `data-nb-collection-mode="load_more"`;
  - временный widget bind `#224` и bind page `#1036` разместили блок на homepage в `pos_38`;
  - публичная homepage отрендерила `bento_feed` без `Fatal error`, `Warning` и `Notice`;
  - cleanup удалил и widget bind, и временный block без хвостов.
- Family registry и checklist обновлены: для `bento_feed` `Live smoke` теперь `yes`.
- Residual risk после этого прохода уже не архитектурный, а визуальный: ручная device-level проверка mobile layout и UX кнопки `Показать ещё` на настоящем телефоне всё ещё остаётся желательной финальной проверкой.

## Bento Feed manual mobile smoke

- Для короткого ручного smoke создан временный live block `#133` и widget bind `#225` в `pos_38`, затем после проверки оба объекта удалены штатно; cleanup подтверждён повторной загрузкой homepage без `bento_feed` DOM markers и без smoke title.
- В live editor на [admin/controllers/edit/nordicblocks/block_edit/133](admin/controllers/edit/nordicblocks/block_edit/133) включён штатный toggle `Мобильный`.
- После переключения mobile canvas дал честный preview-width `390px`; внутри preview `bento_feed` перешёл в одну колонку (`gridTemplateColumns` свёлся к одному track), при этом header сохранился, нижняя кнопка `Показать ещё` осталась видимой, а initial visible cards остались равны `2` из `4`.
- На публичной homepage подтверждён functional smoke для того же временного placement: блок рендерился без `Fatal error`, `Warning` и `Notice`, а кнопка `Показать ещё` расширяла видимый список с `2` до `4` карточек.
- Ограничение этого прохода: встроенный browser tooling в текущей среде не применил narrow viewport к публичной вкладке, поэтому именно public mobile width не был честно эмулирован в браузере, хотя editor mobile preview и public runtime behavior подтверждены отдельно.

## V2 planning and slider family docs

- В отдельный future-facing документ вынесен roadmap версии 2.0: [docs/nordicblocks/NORDICBLOCKS-V2-ROADMAP.md](docs/nordicblocks/NORDICBLOCKS-V2-ROADMAP.md).
- В отдельный family spec вынесена следующая продуктовая группа блоков `slider`: [docs/nordicblocks/SLIDER-FAMILY-V1.md](docs/nordicblocks/SLIDER-FAMILY-V1.md).
- На уровне документации зафиксировано, что `slider family` рассматривается как первая осознанно новая V2-линейка после news/catalog wave.
- На этом шаге код для slider blocks ещё не открывался: сначала зафиксирован family-level vocabulary сущностей, вкладок, runtime-границ и рекомендуемый первый block type `cards_slider`.

## Slider family next-step closure

- После уточнения продуктовых требований зафиксировано, что все три slider blocks проектируются как `full-width` family с viewport `100%`, starter demo content, `content_list` path и обязательным mobile swipe.
- Для первого truthful implementation шага создан отдельный rollout-map [docs/nordicblocks/CARDS-SLIDER-ROLLOUT-MAP-2026-04-20.md](docs/nordicblocks/CARDS-SLIDER-ROLLOUT-MAP-2026-04-20.md).
- Для первого block type создан implementation-grade spec [docs/nordicblocks/CARDS-SLIDER-V1.md](docs/nordicblocks/CARDS-SLIDER-V1.md).
- Для shared editor platform создан отдельный vocabulary doc [docs/nordicblocks/SLIDER-INSPECTOR-VOCABULARY-V1.md](docs/nordicblocks/SLIDER-INSPECTOR-VOCABULARY-V1.md), чтобы `cards_slider` не открывался через block-private shell logic.
- На этом шаге код по `cards_slider`, `hero_slider` и `stories_slider` ещё не начинался: сначала закрыт документальный pre-implementation contour по порядку `family rules -> rollout-map -> spec -> shared vocabulary`.

## Slider shared inspector vocabulary rollout

- Перед кодовыми изменениями создан checkpoint `snapshot/20260420-094155`, DB backup — `backups/db/builders-20260420-094153.sql.gz`.
- Shared registry vocabulary для slider family добавлен в live и package mirror `InspectorDefinitionRegistry`:
  - введены canonical slider entities `viewport`, `track`, `slide`, `navigation`, `prevButton`, `nextButton`, `pagination`, `progress`, `slideSurface`, `slideMedia`, `slideEyebrow`, `slideTitle`, `slideText`, `slideMeta`, `slidePrimaryAction`, `slideSecondaryAction`;
  - введены shared entity groups для `sliderNavigation`, `slides` и `slideActions`;
  - зафиксированы capability flags `hasSlides`, `hasSliderNavigation`, `hasSliderPagination`, `hasSliderProgress`, `hasMobileSwipe`, `hasAutoplay`, `hasLoop`, `hasContentListSource`;
  - заведены panel/control keys для content/design/layout/data vocabulary slider family.
- Этот шаг сознательно ограничен registry-layer: shared shell renderers для новых slider control components ещё не включались, чтобы не подменить pre-scaffold vocabulary rollout полуреализованным block UI.