{
  "kind": "nordicbuilder.contract",
  "schema_version": "1.0",
  "key": "binding-options",
  "title": "Binding Options",
  "contract_kind": "nordicbuilder.binding-options",
  "bridge_kind": "landingbuilder.binding-options",
  "storage_target": "bindings.options_json",
  "description": "Runtime options contract для binding-level matching, fallback и preview rules.",
  "required_fields": [
    "schema_version",
    "matching",
    "fallback",
    "rules"
  ],
  "notes": [
    "concrete binding docs may omit key and title in runtime storage",
    "must not duplicate sql columns like page_id or adapter_key",
    "zones.enabled must follow adapter manifest"
  ]
}